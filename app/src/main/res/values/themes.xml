<resources>
    <!-- Base application theme. -->
    <style name="Theme.RealmImade" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/holo_red_dark</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.RealmImade.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/holo_red_dark</item>
    </style>

</resources>